# AGENTS.md

## Project Overview

本项目由多人协作开发，Agent 的职责是辅助完成代码开发、问题排查、测试验证、文档维护和项目交付相关工作。

Agent 在开始任何任务前，必须先理解当前任务目标、项目目录结构和相关上下文。不得在未确认的情况下擅自扩展需求、改变项目方向或修改无关文件。

Agent 必须尊重用户已有文件和修改。工作区中已有的变更默认视为用户或其他成员的工作成果，不得擅自回滚、覆盖、删除或重命名。

如果任务描述不清楚，Agent 应主动询问用户，不要自行猜测处理。

Agent 必须遵守以下原则：

- 先理解上下文，再进行修改
- 保持改动范围清晰可控
- 不做与当前任务无关的重构
- 不擅自删除、覆盖或移动文件
- 不擅自执行打包、发送邮件或上传平台等外部操作
- 不把临时想法、未确认需求或个人推测写入正式文档
- 非必要情况下不要创建脚本文件
- 如果必须创建临时脚本，使用完成后必须立即删除

每次处理任务时，Agent 应遵循：

1. 阅读任务描述。
2. 查看相关目录和文件。
3. 明确本次修改范围。
4. 如有不确定点，先询问用户。
5. 修改前说明准备改什么。
6. 使用最小必要改动完成任务。
7. 运行必要测试或检查。
8. 汇报修改内容、验证结果和遗留问题。

## Build & Commands

Agent 执行命令前，应先判断命令目的、影响范围和风险。

允许直接执行：

- 查看目录结构
- 搜索代码
- 阅读文件
- 查看 Git 状态、差异和提交历史
- 运行测试
- 运行类型检查
- 运行格式检查
- 编译或构建项目
- 启动本地开发服务

需要谨慎执行：

- 安装依赖
- 修改依赖版本
- 数据库迁移
- 批量格式化
- 生成大量构建产物
- 清理缓存或临时目录
- 调用外部服务接口

必须先获得用户明确确认：

- 删除文件或目录
- 覆盖已有文件
- 批量重命名或移动文件
- 执行最终打包
- 生成最终压缩包
- 发送邮件
- 上传到外部平台
- 修改生产配置
- 修改数据库结构
- 清空、重置或迁移真实数据
- 执行可能影响用户数据的命令
- 执行高风险 Git 操作

禁止在未确认情况下执行：

```text
git reset --hard
git checkout --
git clean
强制推送
删除分支
回滚用户修改
```

如果命令失败，Agent 应说明失败原因，并区分是环境问题、依赖问题、代码问题、权限问题还是外部服务问题。

## Code Style

Agent 修改代码时，必须遵守项目已有风格。

基本要求：

- 优先沿用现有目录结构
- 优先沿用现有命名方式
- 优先沿用现有技术栈
- 优先复用项目已有工具函数、组件和模块
- 保持模块职责清晰
- 避免跨层调用不该直接依赖的模块
- 不把临时逻辑写进核心模块
- 不把多个职责混在同一个文件或函数中
- 保持改动范围小而清晰
- 不引入不必要的新依赖
- 不引入不必要的复杂抽象
- 不批量格式化无关文件
- 不做与当前任务无关的重构

修改前必须先阅读相关代码上下文，不要只根据文件名猜测用途。

如果发现已有代码存在问题：

- 只处理与当前任务直接相关的问题
- 不借当前任务扩大修改范围
- 如果问题影响较大，应先向用户说明并确认处理方式

涉及前端时，应遵守：

- 复用已有组件
- 复用已有样式规范
- 不随意改变整体布局
- 不引入与项目风格不一致的 UI
- 表单、按钮和提示文案应清晰易懂
- 危险操作必须有明确确认提示

涉及接口或外部集成时，应遵守：

- 接口命名清晰
- 请求参数明确
- 返回格式一致
- 错误信息可理解
- 权限校验不能省略
- 外部服务调用必须有失败处理

注释规范：

- 为每个源文件中的函数、类、复杂逻辑分支添加行内注释或块注释。
- 复杂逻辑分支包括循环、条件判断、递归和多步骤状态处理。
- 公共 API 函数必须在函数上方添加 JSDoc 风格注释，说明函数功能、参数类型及含义、返回值类型及含义。
- 复杂算法实现超过 10 行时，必须在算法块开始处添加注释，解释算法思路与关键步骤。
- 配置文件中的每个键或配置项都应添加注释，说明用途和取值范围；如文件格式不支持注释，应在相邻说明文档或示例配置中补充说明。
- 与业务逻辑相关的常量或魔法数字，必须在定义旁边添加注释解释其业务含义。
- 注释语言统一使用中文。
- 注释内容应简洁明确，避免重复描述代码本身。
- 为整个项目中的源文件执行开发任务时，Agent 应同步检查并补充符合上述规则的注释。

Agent 应避免在项目中留下以下文件：

```text
*.tmp
*.bak
*.log
.DS_Store
Thumbs.db
__pycache__/
node_modules/
dist/
build/
.env
```

如上述文件确实属于项目要求，应以用户说明为准。

## Testing

Agent 完成代码修改后，应根据改动范围运行必要检查。

优先执行：

- 单元测试
- 集成测试
- 类型检查
- Lint 检查
- 构建检查
- 相关功能的手动验证

如果无法运行测试，必须说明原因。

如果测试失败，Agent 应：

- 先定位失败原因
- 区分是本次修改导致还是项目已有问题
- 不得为了通过测试而删除、跳过或弱化测试
- 不得隐藏失败结果
- 不得伪造测试通过

涉及以下能力时，必须重点验证：

- 权限控制
- 文件上传
- 文件下载
- 文件删除
- 文件检查
- 打包导出
- 邮件发送
- 平台提交
- 数据库读写
- 第三方接口
- 路径安全
- 异常处理

涉及大文件、上传、下载、打包、导出或批量处理时，应关注：

- 避免一次性读取过大的文件到内存
- 避免无意义的重复扫描
- 避免阻塞主线程或主请求
- 大文件处理应有明确限制
- 长时间任务应提供状态反馈
- 批量操作应考虑失败恢复

## Security

Agent 必须优先保护项目文件、用户数据和敏感信息。

禁止行为：

- 将密码、Token、Cookie、邮箱授权码写入源码
- 将敏感配置提交到仓库
- 在日志中输出敏感信息
- 执行用户上传的脚本
- 信任未经校验的文件名或路径
- 允许路径穿越
- 未确认就删除用户文件
- 未确认就发送邮件
- 未确认就上传外部平台
- 未确认就执行最终打包
- 未确认就访问、修改或删除项目目录外部文件

处理文件路径时，必须限制在项目目录内。

必须防范以下路径：

```text
../
..\
C:\
/etc/
~/
```

涉及上传、下载、复制、移动、删除、解压、打包、导出时，必须检查：

- 文件类型
- 文件大小
- 文件名合法性
- 路径是否合法
- 是否会覆盖已有文件
- 是否会访问项目目录外部
- 是否包含不应提交的临时文件或敏感文件

涉及打包、导出、邮箱提交或平台提交时，Agent 必须遵守：

- 不得擅自执行最终打包
- 不得擅自生成最终压缩包
- 不得擅自发送邮件
- 不得擅自上传平台
- 不得提前压缩成员文件
- 不得生成多层压缩包
- 不得把压缩包作为中间开发产物留在项目中

只有在用户明确要求时，Agent 才能执行打包、导出、发送或上传相关操作。

执行前必须确认：

- 操作对象
- 输出位置
- 文件命名
- 是否覆盖已有文件
- 是否发送到外部系统
- 是否会影响用户数据

涉及邮箱、平台、对象存储或其他第三方服务时：

- 凭据必须来自环境变量或安全配置
- 发送或上传前必须获得用户确认
- 不在日志中输出敏感信息
- 错误信息不能泄露敏感凭据
- 不在测试中调用真实外部提交接口，除非用户明确要求

## Configuration

Agent 不应擅自修改项目配置、依赖配置、部署配置或运行环境配置。

配置类文件包括但不限于：

```text
.env
.env.local
.env.production
package.json
package-lock.json
pnpm-lock.yaml
yarn.lock
requirements.txt
pyproject.toml
vite.config.*
next.config.*
tsconfig.json
docker-compose.yml
Dockerfile
```

修改配置前应确认：

- 修改目的
- 影响范围
- 是否会影响已有开发环境
- 是否会影响部署环境
- 是否需要同步更新文档
- 是否需要用户确认

环境变量应只提供示例，不写真实值。

允许提供：

```text
.env.example
```

禁止提交：

```text
.env
.env.local
.env.production
```

如果必须新增配置项，Agent 应说明：

- 配置项名称
- 配置项用途
- 默认值
- 是否必填
- 缺失时系统如何处理

修改功能、配置或流程时，Agent 应同步检查是否需要更新文档。

可更新的文档包括：

- `README.md`
- 开发说明
- 接口说明
- 环境配置说明
- 测试说明
- 部署说明
- 使用说明

文档更新应准确、简洁，不写未经确认的内容。

生成或更新 `spec`、`plan`、`tasks` 相关文档时，必须在文档末尾添加最后生成时间戳。

时间戳格式：

```text
最后生成时间：YYYY-MM-DD HH:mm:ss
```

Agent 的最终原则是：先理解，再修改；小步改动，及时验证；危险操作先确认；不留下临时痕迹。
